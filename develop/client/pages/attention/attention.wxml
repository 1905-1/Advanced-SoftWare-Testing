<!--pages/attention/attention.wxml-->

<block wx:for="{{posts_key}}" wx:for-item="item">
  <view wx:if="{{concerned[item.authorId]}}" class="post_author" >
    <image class="author_img" src="{{item.author_img}}"></image>
    <text>{{item.author_name}}</text>
  </view>
</block>